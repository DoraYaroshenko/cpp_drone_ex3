write readme

rename res to resolution in CollisionUtils.cpp. redundant XLength constructor.

change value to VoxelOccupance enum value in void Logger::logVoxel(int x, int y, int z, int value) in Logger.cpp

make variables names more indicative in bool isVoxelKnownSafe and getUnknownVoxelsInSphere in MappingAlgorithmImpl.cpp

getUnknownVoxelsInSphere update count to contains in MappingAlgorithmImpl.cpp

divide handleFollowPath in MappingAlgorithmImpl.cpp to several functions

maybe divide runMission in MissionControlImpl.cpp into several funcs
If we were to refactor this to adhere strictly to the Single Responsibility Principle, we could divide it into three private helper methods:
1. Extract the main loop body into executeSingleStep You could pull the inside of the for loop into a method that returns a boolean indicating whether the mission should continue.
2. Extract Map Saving into saveOutputMap The try-catch block at the end that saves the .npy file can easily be its own method.
3. Extract logError to a private class method Instead of a lambda that captures everything, make it a proper member function.

divide step in DroneControlImpl.cpp to smaller functions

change using namespace common in MockGPS.h to smaller import to prevent namespace pollution
very weird usings in include/Simulator in general

why did we put all algorithm includes in include, mission control includes in include, but simulator includes in include/simulator

more indicative method names in Map3DImpl.cpp

Maybe can divide compare in MapsComparison.cpp into smaller functions

Build:
cmake --preset default
cmake --build --preset default

Comparative run:
./build/default/Simulator/simulator_330371063_324976703 -comparative simulation=inputs/sim_compose.yaml mission_control_folder=build/default/MissionControl algorithm=build/default/Algorithm/Algorithm_Simple_330371063_324976703.so

Competitive run:
./build/default/Simulator/simulator_330371063_324976703 -competition simulation=inputs/sim_compose.yaml mission_control=build/default/MissionControl/MissionControl_330371063_324976703.so algorithms_folder=build/default/Algorithm